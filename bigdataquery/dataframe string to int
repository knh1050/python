voc에 bigdataquery 특정 string 형식의 컬럼을 비교 연산자로 범위 조건 설정해서 조회하고 싶다함.

딕셔너리 형태로 값을 받음
param = {col1 : 'aa111', col2 : 'qqqq'}

그러면 bdq에서 impala로 값에 따라 sql문 변환하여 던져줌

위의 예시대로면 impala에서는 아래처럼 변환됨.
select * from db.tablenm where col1 = 'aa111' and col2 = 'qqqq'; 


#문제
다시 voc내용으로 돌아오면
col1은 앞에 영어 2글자 뒤에 숫자값들이 들어있음 

col1
'aa1111'
'aa1112'
'aa1113'
'aa1111'
...

이중에서 앞의 영어가 'aa'이고 뒤의 숫자는 1112에서 1114사이 값만 조회하고 싶어함


#문제 해결과정
bdq에서 딕셔너리 형태로 방법을 어렵게 어렵게 구현해봤자
impala에서는 해당 컬럼 string 형식이라 비교 연산자가 안먹힘
문법 오류는 안나지만 원하는 값이 아닌 이상한 겂들이 들어있음

그러므로 데이터는 전체 받아오고 그중에서 해당 조건에 맞는 데이터들만 추출해야 될거같음.
(주의할점: impala에서는 쿼리제한시간 15분이 있는데 데이터 전체를 다받아서 추출하자고하면 지금까지 필요한 범위만 조회하라고 답변했던 기조에 위배됨)

파이썬 dataframe 함수 사용해서 슬라이싱으로 앞에 글자가 'aa'일경우 또 뒤에 숫자만 비교하는 형식으로 알려줘야할거같음
